import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:fluttersampleapp/app/modules/contact/component/add_contact/add_contact_page.dart';

main() {
  testWidgets('AddContactPage has title', (WidgetTester tester) async {
    //  await tester.pumpWidget(buildTestableWidget(AddContactPage(title: 'AddContact')));
    //  final titleFinder = find.text('AddContact');
    //  expect(titleFinder, findsOneWidget);
  });
}
